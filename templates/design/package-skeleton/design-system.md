# <NAME> — motion-graphics design system

**Status:** DRAFT (promote to ADOPTED when this package moves to
`design/active/`).

## Concept

One paragraph: what world do these graphics come from? (A designer's desk?
A chalkboard? A terminal?) Name what is FORBIDDEN as clearly as what is
allowed — the bans are what keep AI-generated graphics from drifting
generic.

## Tokens

The machine half lives in `tokens.ts` (BaseTokens contract + any extra
exports). Document here anything tokens can't express: modes, laws,
combination rules.

## Typography

| Role | Face | Use |

## Element inventory

Numbered list of the kit's elements (cards, stamps, figures, tables,
annotations…) with one line each on how they behave.

## Motion language

Which verbs are allowed (draw, swipe, stamp…), which are banned (bounce,
glow, 3D…), standard durations, and the annotation-after-target rule.

## Layout integration

How the kit sits in the five layouts (Overlay cells, Split content side,
FullGraphics pages, chapter cards).
